# Testing

```
# Generate a test entwine index.
npm run generate-test-data

# Start the server.  For debug mode, instead use:
#   npm run debug
npm start

# Then, from another window, run the tests.
npm run test
```

